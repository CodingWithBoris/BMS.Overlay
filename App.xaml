<Application x:Class="BMS.Overlay.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BMS.Overlay"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- ═══ TACTICAL COLOR RESOURCES (GLOBAL) ═══ -->
        <DrawingBrush x:Key="OverlayDottedTexture" TileMode="Tile"
                      Viewport="0,0,6,6" ViewportUnits="Absolute"
                      Viewbox="0,0,6,6" ViewboxUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#1C1C1C">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry Center="1,1" RadiusX="0.6" RadiusY="0.6"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>

        <SolidColorBrush x:Key="GoldButtonBackground" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="GoldButtonBackgroundHover" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="GoldBorderBrush" Color="#6B4F00"/>
        <SolidColorBrush x:Key="GoldBorderBrushHover" Color="#D4880A"/>
        <SolidColorBrush x:Key="GoldTextBrush" Color="#B8860B"/>
        <SolidColorBrush x:Key="GoldTextBrushHover" Color="#FFB300"/>

        <SolidColorBrush x:Key="DarkButtonBackground" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="DarkButtonBackgroundHover" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="DarkBorderBrush" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="DarkBorderBrushHover" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="DarkTextBrush" Color="#A0A0A0"/>
        <SolidColorBrush x:Key="DarkTextBrushHover" Color="#FFFFFF"/>

        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="HeaderLineBrush" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#D8D8D8"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#A0A0A0"/>
        <SolidColorBrush x:Key="DimTextBrush" Color="#6A6A6A"/>

        <!-- ═══ TACTICAL BUTTON STYLES (GLOBAL) ═══ -->
        <Style x:Key="TacticalGoldButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource GoldTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource GoldButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GoldBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Rectangle Fill="{StaticResource OverlayDottedTexture}" Opacity="0.35" IsHitTestVisible="False"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource GoldBorderBrushHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource GoldTextBrushHover}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#D4880A" BlurRadius="10" ShadowDepth="0" Opacity="0.65"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.92"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TacticalDarkButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource DarkButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Rectangle Fill="{StaticResource OverlayDottedTexture}" Opacity="0.35" IsHitTestVisible="False"/>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource DarkBorderBrushHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource DarkTextBrushHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.92"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TacticalTabButton" TargetType="Button" BasedOn="{StaticResource TacticalDarkButton}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource GoldTextBrush}"/>
                    <Setter Property="Background" Value="{StaticResource GoldButtonBackground}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource GoldBorderBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ═══ TACTICAL CHECKBOX — flat, sharp, gold check ═══ -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="box" Width="16" Height="16"
                                    Background="{StaticResource PanelBackgroundBrush}"
                                    BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1"
                                    VerticalAlignment="Center" Margin="0,0,6,0">
                                <TextBlock x:Name="check" Text="✓" FontSize="12" FontWeight="Bold"
                                           Foreground="{StaticResource DarkTextBrushHover}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Visibility="Collapsed" Margin="0,-1,0,0"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="check" Property="Foreground" Value="{StaticResource GoldTextBrush}"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource GoldBorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource DarkBorderBrushHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ TACTICAL COMBOBOX — flat, dark, no chrome ═══ -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="MinWidth" Value="180"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleBtn" Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="toggleBorder"
                                                Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="24"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="1">
                                                    <TextBlock Text="▾" Foreground="{StaticResource DimTextBrush}" FontSize="12"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="toggleBorder" Property="BorderBrush" Value="{StaticResource DarkBorderBrushHover}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <TextBlock x:Name="ContentSite" IsHitTestVisible="False"
                                       Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="{StaticResource MutedTextBrush}" FontSize="12" FontWeight="Bold"
                                       Margin="8,5,28,5" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="None">
                                <Border x:Name="DropDownBorder" Background="#0D0D0D"
                                        BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200">
                                    <ScrollViewer CanContentScroll="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#0D0D0D"/>
                        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="itemBd" Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}" BorderThickness="0">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="itemBd" Property="Background" Value="#141414"/>
                                            <Setter Property="Foreground" Value="{StaticResource DarkTextBrushHover}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="itemBd" Property="Background" Value="#141414"/>
                                            <Setter Property="Foreground" Value="{StaticResource DarkTextBrushHover}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
