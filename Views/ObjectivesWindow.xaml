<Window x:Class="BMS.Overlay.Views.ObjectivesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BMS Objectives"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        Opacity="0">
    <Border Background="Transparent" BorderBrush="#2A3A2A" BorderThickness="0">
        <DockPanel>
            <!-- Header -->
            <Border DockPanel.Dock="Top" Background="Transparent" BorderBrush="#2A3A2A"
                    BorderThickness="0" Padding="10,6">
                <TextBlock Text="MISSION OBJECTIVES" Foreground="White"
                           FontSize="11" FontWeight="Bold"/>
            </Border>

            <!-- Objectives list -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="6,4">
                <StackPanel x:Name="ObjectivesStack" Margin="2,2,2,4"/>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
