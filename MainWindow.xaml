<Window x:Class="BMS.Overlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    WindowStyle="None" AllowsTransparency="True"
    Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        Left="0" Top="0" Width="500" MaxWidth="500"
        Title="BMS Overlay"
        Closing="Window_Closing">
    <Window.Resources>
        <LinearGradientBrush x:Key="GoldButtonBackground" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#111113" Offset="0"/>
            <GradientStop Color="#2A2315" Offset="0.5"/>
            <GradientStop Color="#111113" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="GoldButtonBackgroundHover" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#1A1A1D" Offset="0"/>
            <GradientStop Color="#3A311B" Offset="0.5"/>
            <GradientStop Color="#1A1A1D" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="GoldBorderBrush" Color="#D4AF37"/>
        <SolidColorBrush x:Key="GoldTextBrush" Color="#E3C341"/>

        <LinearGradientBrush x:Key="DarkButtonBackground" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#020407" Offset="0"/>
            <GradientStop Color="#06090E" Offset="0.5"/>
            <GradientStop Color="#020407" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="DarkButtonBackgroundHover" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#05080D" Offset="0"/>
            <GradientStop Color="#0A0E15" Offset="0.5"/>
            <GradientStop Color="#05080D" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="DarkBorderBrush" Color="#252B33"/>
        <SolidColorBrush x:Key="DarkTextBrush" Color="#6F747D"/>

        <Style x:Key="OverlayGoldButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource GoldTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource GoldButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GoldBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource GoldButtonBackgroundHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OverlayDarkButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="Background" Value="{StaticResource DarkButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkButtonBackgroundHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.92"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OverlayTabButtonStyle" TargetType="Button" BasedOn="{StaticResource OverlayDarkButtonStyle}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource GoldTextBrush}"/>
                    <Setter Property="Background" Value="{StaticResource GoldButtonBackground}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource GoldBorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Slim white scrollbar -->
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="20"/>
            <Setter Property="MinWidth" Value="20"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border Background="White" Width="20" Height="75"
                                                                    CornerRadius="0"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="MinHeight" Value="20"/>
                    <Setter Property="Width" Value="Auto"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Background="Transparent">
        <!-- Main Panel (visible when not minimized) -->
        <Border x:Name="MainPanel" Background="#B0080B12" CornerRadius="0,8,8,0"
                Visibility="Visible">
            <DockPanel>
                <!-- Tab Bar -->
                <Grid DockPanel.Dock="Top" Margin="6,5,6,0" MaxWidth="500" HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                        <Button x:Name="BmsTabButton" Grid.Column="0" Content="BMS" Height="44"
                            Style="{StaticResource OverlayTabButtonStyle}"
                            Click="OnBmsTab_Click"/>
                        <Button x:Name="OptionsTabButton" Grid.Column="2" Content="BMS OPTIONS" Height="44"
                            Style="{StaticResource OverlayTabButtonStyle}"
                            Click="OnOptionsTab_Click"/>
                </Grid>
                
                <!-- Bottom Nav -->
                <Grid DockPanel.Dock="Bottom" Margin="6,0,6,5" Height="44" MaxWidth="500" HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                        <Button x:Name="PrevButton" Grid.Column="0" Content="&lt;" Height="44" Click="OnPrev_Click"
                            Style="{StaticResource OverlayDarkButtonStyle}"/>
                        <Button x:Name="MinimizeButton" Grid.Column="2" Content="-" Height="44"
                            Click="OnMinimize_Click" Style="{StaticResource OverlayDarkButtonStyle}"/>
                        <Button x:Name="NextButton" Grid.Column="4" Content="&gt;" Height="44" Click="OnNext_Click"
                            Style="{StaticResource OverlayDarkButtonStyle}"/>
                </Grid>
                
                <!-- Content -->
                <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden" Background="Transparent"/>
            </DockPanel>
        </Border>
        
        <!-- Minimized Bar -->
        <Button x:Name="RestoreBar" Content="+"
                VerticalAlignment="Bottom" Height="44" Width="500" MaxWidth="500" HorizontalAlignment="Stretch"
                Visibility="Collapsed"
            Click="OnRestore_Click" Style="{StaticResource OverlayDarkButtonStyle}" FontSize="20"/>
    </Grid>
</Window>
